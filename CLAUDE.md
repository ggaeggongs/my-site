# 개인 웹사이트 프로젝트 지침

> 이 파일은 이 폴더에서 작업할 때 Claude가 항상 지켜야 하는 규칙입니다.

## 작업자 정보
- 사용자는 **코딩을 모르는 비개발자**입니다.
- 모든 설명은 **쉬운 한국어**로 해주세요. 전문 용어는 최대한 풀어서 설명합니다.

## 도구 구성 (작업 흐름)
1. **Claude 데스크톱 앱(Claude Code)** 으로 파일을 만들고 수정합니다.
2. **GitHub Desktop** 으로 Commit(저장) · Push(업로드) 합니다.
3. **Vercel** 이 GitHub 변경을 감지해 **자동으로 배포**합니다.

## 기술 제약 (반드시 지킬 것)
- 사용자 컴퓨터에는 **터미널이나 Node.js 같은 개발 도구가 없습니다.**
- **설치(npm)나 빌드가 필요한 프레임워크(React, Next.js 등)는 절대 사용하지 마세요.**
- **순수 HTML / CSS / JavaScript** 로만 작업합니다.
- 파일을 **더블클릭하면 브라우저에서 바로 열려야** 합니다.
- 첫 화면 파일 이름은 **`index.html`** 로 합니다.

## 앞으로의 계획 (4주 로드맵)
이 사이트는 4주에 걸쳐 발전시킵니다.
- **1~2주차:** 순수 HTML/CSS/JS로 기본 웹사이트 제작
- **3주차:** Supabase 데이터베이스(DB) 연동
- **4주차:** Claude API를 사용하는 AI 기능(`/api`) 추가

> 참고: 3주차 이후 `/api` 기능은 Vercel의 서버리스 기능을 활용할 예정입니다.
> 이 단계에 도달하기 전까지는 위의 '기술 제약'을 그대로 유지합니다.
